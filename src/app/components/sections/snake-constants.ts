// Figma-based sizing (from your screenshots)
export const GIF_W = 213; // px (desktop GIF width)
export const GIF_H = 180; // px (desktop GIF height)

// Path styling
export const RADIUS = 10; // px corner radius of the snake
export const RAIL_INSET = 16; // px inset of the left/right rail from container edges
export const STROKE = 2; // px snake thickness

// Optional spacing helpers (tune if your rows change)
export const TAIL_AFTER_LAST = 44; // px tail length after last row
